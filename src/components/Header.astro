---
const navLinks = [
    {title: 'Home', url: '/', current: false},
    {title: 'About', url: '/about/', current: false}
]
---
<header>
    <h1>
        Tech Study Blog | テクスタ
    </h1>
    <nav>
        <ul>
            {
                navLinks.map((menu) =>{
                    return (
                        <li>
                            <a href={menu.url}>{menu.title}</a>
                        </li>
                    )
                })
            }
        </ul>
    </nav>
</header>